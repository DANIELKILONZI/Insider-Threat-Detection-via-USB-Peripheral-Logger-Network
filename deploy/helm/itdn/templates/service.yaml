apiVersion: v1
kind: Service
metadata:
  name: {{ include "itdn.fullname" . }}
  labels:
    {{- include "itdn.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "itdn.selectorLabels" . | nindent 4 }}
