# ── Server dependencies ───────────────────────────────────────────────────────
flask>=3.0,<4.0
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0

# ── Structured JSON logging ───────────────────────────────────────────────────
python-json-logger>=2.0,<4.0

# ── Audit-log encryption at rest (AES-GCM) ───────────────────────────────────
cryptography>=41.0,<46.0

# ── Agent optional dependencies (install on Linux endpoints) ──────────────────
# pyudev>=0.24        # Linux udev USB events (highly recommended on Linux)
# wmi>=1.5.1          # Windows USB/Bluetooth enumeration (Windows only)
# pythoncom>=302      # Windows COM initialisation for WMI event monitor

# ── PostgreSQL backend (optional) ─────────────────────────────────────────────
# psycopg2-binary>=2.9  # install only when ITDN_DATABASE_URL points to PostgreSQL

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0
pytest-timeout>=2.3
# pytest-benchmark>=4.0  # optional – only needed when running tests/test_load.py

# bcc>=0.29  # Linux eBPF USB monitoring (optional, requires kernel headers)
